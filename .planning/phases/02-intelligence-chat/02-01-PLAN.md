---
phase: 02-intelligence-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/database.py
  - backend/models.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "backend/requirements.txt lists ta>=0.11.0 and NOT pandas-ta"
    - "backend/requirements.txt lists newsapi-python>=0.2.7, finnhub-python>=2.4.28, anthropic>=0.102.0"
    - "SQLite schema includes news_cache and analyst_cache tables"
    - "HoldingResponse Pydantic model includes rec, rsi_14, macd, macd_signal, macd_histogram, sma_50, sma_200, analyst_rating, analyst_target, analyst_num, ai_narrative fields"
    - "BriefingResponse Pydantic model includes news Optional[dict] field"
    - "ChatRequest and ChatResponse Pydantic models exist in models.py"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Dependency declarations for all Phase 2 libraries"
      contains: "ta>=0.11.0"
    - path: "backend/database.py"
      provides: "news_cache and analyst_cache table creation"
      contains: "news_cache"
    - path: "backend/models.py"
      provides: "Extended Pydantic models with Phase 2 fields"
      exports: ["HoldingResponse", "BriefingResponse", "ChatRequest", "ChatResponse"]
  key_links:
    - from: "backend/models.py HoldingResponse"
      to: "backend/core/data_fetcher.py + backend/core/ai_synthesis.py"
      via: "rec, rsi_14, analyst_rating, ai_narrative fields consumed by downstream plans"
      pattern: "rec: Optional\\[str\\] = None"
    - from: "backend/database.py create_schema()"
      to: "news_cache and analyst_cache tables"
      via: "CREATE TABLE IF NOT EXISTS"
      pattern: "news_cache"
---

<objective>
This plan establishes the data contracts and database schema that all subsequent Phase 2 plans depend on. It replaces the incompatible pandas-ta dependency, installs the four new libraries, creates two new SQLite cache tables, extends HoldingResponse with 11 new fields (signals + analyst + AI synthesis), extends BriefingResponse with a news field, and adds ChatRequest/ChatResponse models.

Purpose: Every downstream plan in Phase 2 imports from models.py and writes to news_cache/analyst_cache. Getting contracts right here prevents rework in Plans 02–04.

Output: Updated requirements.txt, database.py with two new tables, models.py with extended schemas and new chat models.
</objective>

<execution_context>
@/Users/sy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sy/Desktop/Learning/InvestingApp/.planning/ROADMAP.md
@/Users/sy/Desktop/Learning/InvestingApp/.planning/phases/02-intelligence-chat/02-CONTEXT.md
@/Users/sy/Desktop/Learning/InvestingApp/.planning/phases/02-intelligence-chat/02-RESEARCH.md
</context>

<interfaces>
<!-- Current state of files being modified. Read these before touching anything. -->

From backend/requirements.txt (current — pandas-ta must be replaced):
```
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
yfinance>=0.2.40
pandas>=1.5.0
pandas-ta>=0.3.14b      ← REMOVE THIS LINE
apscheduler>=3.10.0
python-dotenv>=1.0.0
pydantic>=2.0.0
pytz>=2024.1
requests>=2.31.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
```

From backend/models.py (current HoldingResponse — Phase 1 delivered):
```python
class HoldingResponse(BaseModel):
    id: Optional[int] = None
    ticker: str
    isin: Optional[str] = None
    name: Optional[str] = None
    quantity: float
    avg_buy: float
    current_price: Optional[float] = None
    pl: float
    pl_pct: float
    pl_usd: float = 0.0
    currency: str
    region: Optional[str] = None
    asset_type: Optional[str] = None
    broker: str
    price_date: Optional[str] = None

class BriefingResponse(BaseModel):
    portfolio: dict
    indices: dict
    fx: dict
    generated_at: str
    briefing_date: str
    fetched_at: str
```

From backend/database.py (existing technical_indicators table — already exists, NO CHANGE NEEDED):
```sql
CREATE TABLE IF NOT EXISTS technical_indicators (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ticker TEXT NOT NULL, date TEXT NOT NULL,
    rsi_14 REAL, macd REAL, macd_signal REAL, macd_histogram REAL,
    bb_upper REAL, bb_mid REAL, bb_lower REAL,
    sma_50 REAL, sma_200 REAL, ema_20 REAL,
    computed_at TEXT DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(ticker, date)
)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace pandas-ta with ta library and add Phase 2 dependencies</name>
  <files>backend/requirements.txt</files>
  <read_first>
    - backend/requirements.txt — read current contents before modifying; pandas-ta line must be removed, not just commented
  </read_first>
  <action>
    Edit backend/requirements.txt: remove the line `pandas-ta>=0.3.14b` entirely (it has no Python 3.9 compatible release on PyPI — confirmed in RESEARCH.md). Add four new lines after the `pandas>=1.5.0` line:

    ```
    ta>=0.11.0
    newsapi-python>=0.2.7
    finnhub-python>=2.4.28
    anthropic>=0.102.0
    ```

    After editing, install the updated requirements into the project's Python environment:
    `pip3 install -r backend/requirements.txt`

    Verify the install: run `python3 -c "import ta; import newsapi; import finnhub; import anthropic; print('all ok')"` — must print "all ok" with no ImportError.
  </action>
  <verify>
    <automated>grep -v '^#' backend/requirements.txt | grep -c 'pandas-ta' | grep -q '^0$' && grep 'ta>=0.11.0' backend/requirements.txt && grep 'newsapi-python>=0.2.7' backend/requirements.txt && grep 'finnhub-python>=2.4.28' backend/requirements.txt && grep 'anthropic>=0.102.0' backend/requirements.txt && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pandas-ta' backend/requirements.txt` returns 0 (line fully removed, not commented)
    - `grep 'ta>=0.11.0' backend/requirements.txt` exits 0
    - `grep 'newsapi-python>=0.2.7' backend/requirements.txt` exits 0
    - `grep 'finnhub-python>=2.4.28' backend/requirements.txt` exits 0
    - `grep 'anthropic>=0.102.0' backend/requirements.txt` exits 0
    - `python3 -c "import ta; import newsapi; import finnhub; import anthropic; print('all ok')"` prints "all ok"
  </acceptance_criteria>
  <done>requirements.txt updated with no pandas-ta entry and all four Phase 2 libraries present and importable</done>
</task>

<task type="auto">
  <name>Task 2: Add news_cache and analyst_cache tables to SQLite schema</name>
  <files>backend/database.py</files>
  <read_first>
    - backend/database.py — read the full file to understand create_schema() structure and existing table creation pattern (CREATE TABLE IF NOT EXISTS with UNIQUE constraint)
  </read_first>
  <action>
    Add two new `CREATE TABLE IF NOT EXISTS` blocks inside `create_schema()` in backend/database.py, following the exact same pattern as existing tables (parameterized, IF NOT EXISTS, UNIQUE constraint):

    Table 1 — news_cache: columns id (PK AUTOINCREMENT), query TEXT NOT NULL, date TEXT NOT NULL, articles_json TEXT NOT NULL, cached_at TEXT DEFAULT CURRENT_TIMESTAMP, UNIQUE(query, date).

    Table 2 — analyst_cache: columns id (PK AUTOINCREMENT), symbol TEXT NOT NULL, date TEXT NOT NULL, rating TEXT, target_mean REAL, num_analysts INTEGER, cached_at TEXT DEFAULT CURRENT_TIMESTAMP, UNIQUE(symbol, date).

    Place both CREATE TABLE blocks at the end of create_schema(), before conn.commit(). Do NOT alter any existing table definitions.
  </action>
  <verify>
    <automated>python3 -c "
import tempfile, os, sqlite3
from backend.database import create_schema
with tempfile.NamedTemporaryFile(suffix='.db', delete=False) as f:
    db = f.name
try:
    create_schema(db)
    conn = sqlite3.connect(db)
    tables = [r[0] for r in conn.execute(\"SELECT name FROM sqlite_master WHERE type='table'\").fetchall()]
    conn.close()
    assert 'news_cache' in tables, f'news_cache missing from {tables}'
    assert 'analyst_cache' in tables, f'analyst_cache missing from {tables}'
    print('PASS')
finally:
    os.unlink(db)
"</automated>
  </verify>
  <acceptance_criteria>
    - `create_schema(db_path)` call creates a SQLite DB where `SELECT name FROM sqlite_master WHERE type='table'` includes both 'news_cache' and 'analyst_cache'
    - news_cache table has columns: id, query, date, articles_json, cached_at, with UNIQUE(query, date)
    - analyst_cache table has columns: id, symbol, date, rating, target_mean, num_analysts, cached_at, with UNIQUE(symbol, date)
    - Full test suite still passes: `python3 -m pytest tests/ -x -q` exits 0
  </acceptance_criteria>
  <done>database.py create_schema() creates news_cache and analyst_cache tables; existing 48 tests still pass</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pydantic models with Phase 2 fields and chat models</name>
  <files>backend/models.py</files>
  <read_first>
    - backend/models.py — read the complete file; understand current HoldingResponse, BriefingResponse, and all other models before adding fields to avoid breaking existing serialization
  </read_first>
  <action>
    In backend/models.py, make three additions:

    1. Add 11 new Optional fields to HoldingResponse after the existing `price_date` field. All new fields have a default of None. Field names and types exactly as follows:
       - `rec: Optional[str] = None` — values will be "BUY", "HOLD", or "SELL"
       - `rsi_14: Optional[float] = None`
       - `macd: Optional[float] = None`
       - `macd_signal: Optional[float] = None`
       - `macd_histogram: Optional[float] = None`
       - `sma_50: Optional[float] = None`
       - `sma_200: Optional[float] = None`
       - `analyst_rating: Optional[str] = None` — values will be "BUY", "HOLD", or "SELL"
       - `analyst_target: Optional[float] = None`
       - `analyst_num: Optional[int] = None`
       - `ai_narrative: Optional[str] = None`

    2. Add one new Optional field to BriefingResponse after `fetched_at`:
       - `news: Optional[dict] = None` — structure: {"holdings": [...], "india": [...], "germany": [...], "us": [...]}

    3. Add two new top-level Pydantic models at the bottom of models.py (after all existing models):
       ```
       class ChatRequest(BaseModel):
           message: str
           briefing: dict

       class ChatResponse(BaseModel):
           response: str
       ```

    Ensure `Optional` is imported from typing (it already is in Phase 1 models.py — confirm before adding).
  </action>
  <verify>
    <automated>python3 -c "
from backend.models import HoldingResponse, BriefingResponse, ChatRequest, ChatResponse
h = HoldingResponse(ticker='TEST', quantity=1, avg_buy=100, pl=0, pl_pct=0, currency='INR', broker='zerodha')
assert hasattr(h, 'rec'), 'rec missing'
assert hasattr(h, 'rsi_14'), 'rsi_14 missing'
assert hasattr(h, 'analyst_rating'), 'analyst_rating missing'
assert hasattr(h, 'ai_narrative'), 'ai_narrative missing'
b = BriefingResponse(portfolio={}, indices={}, fx={}, generated_at='', briefing_date='', fetched_at='')
assert hasattr(b, 'news'), 'news missing from BriefingResponse'
cr = ChatRequest(message='hello', briefing={})
cr2 = ChatResponse(response='hi')
print('PASS')
"</automated>
  </verify>
  <acceptance_criteria>
    - `from backend.models import HoldingResponse, BriefingResponse, ChatRequest, ChatResponse` imports without error
    - HoldingResponse instantiated with only Phase 1 fields still works (all new fields default to None)
    - HoldingResponse has attributes: rec, rsi_14, macd, macd_signal, macd_histogram, sma_50, sma_200, analyst_rating, analyst_target, analyst_num, ai_narrative
    - BriefingResponse has attribute: news (Optional[dict], default None)
    - ChatRequest model accepts {message: str, briefing: dict}
    - ChatResponse model accepts {response: str}
    - `python3 -m pytest tests/ -x -q` exits 0 (existing tests not broken by new Optional fields)
  </acceptance_criteria>
  <done>models.py extended with 11 HoldingResponse fields, news field on BriefingResponse, and ChatRequest/ChatResponse models; all 48 existing tests still pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements.txt → pip install | Package manager installs code; supply chain risk |
| database.py schema → SQLite file | Schema additions must not break existing data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | requirements.txt pandas-ta removal | mitigate | All packages [OK] per slopcheck audit in RESEARCH.md; no [ASSUMED] or [SUS] packages; standard install proceeds |
| T-02-02 | Tampering | database.py schema change | mitigate | Use CREATE TABLE IF NOT EXISTS — idempotent; existing tables unmodified; run full test suite to verify no regression |
| T-02-03 | Information Disclosure | models.py Optional fields | accept | New Optional[str/float] fields default to None — no PII introduced; Pydantic serializes None fields safely |
| T-02-SC | Tampering | pip install ta/newsapi-python/finnhub-python/anthropic | mitigate | All four packages verified [OK] by slopcheck 0.6.1 in RESEARCH.md; no postinstall scripts detected |
</threat_model>

<verification>
Run after all three tasks complete:

1. `grep -c 'pandas-ta' backend/requirements.txt` → returns 0
2. `python3 -c "import ta, newsapi, finnhub, anthropic; print('imports ok')"` → prints "imports ok"
3. `python3 -m pytest tests/ -x -q` → all 48 tests pass
4. `python3 -c "from backend.models import HoldingResponse, ChatRequest, ChatResponse; print('models ok')"` → prints "models ok"
5. `python3 -c "from backend.database import create_schema; import tempfile, os; f=tempfile.mktemp(suffix='.db'); create_schema(f); import sqlite3; c=sqlite3.connect(f); t=[r[0] for r in c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")]; c.close(); os.unlink(f); assert 'news_cache' in t and 'analyst_cache' in t; print('schema ok')"` → prints "schema ok"
</verification>

<success_criteria>
- pandas-ta fully removed from requirements.txt
- ta, newsapi-python, finnhub-python, anthropic installed and importable
- news_cache and analyst_cache tables created by create_schema()
- HoldingResponse has all 11 new Phase 2 fields (all Optional, default None)
- BriefingResponse has news: Optional[dict] = None
- ChatRequest and ChatResponse models exist and import cleanly
- All 48 existing pytest tests still pass
</success_criteria>

<output>
Create `.planning/phases/02-intelligence-chat/02-01-SUMMARY.md` when done
</output>
